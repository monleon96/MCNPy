# Python
__pycache__/
*.pyc
dist/
docs/build/

# VSCODE
.vscode/
*.code-workspace

# AI tooling
.claude/
.agents/
.codex/
.opencode/

# Personal docs
_README.md
.env
scripts/*.md

# Directories
kika/files/
files/
resources/
kika/ace/files/
kika/cov/files/
kika/sampling/perturbed_ace_files/
kika/sampling/new_perturbed_ace_files/
kika/sampling/new_perturbed_ace_files2/
kika/sampling/test_endf_pert/
kika/sampling/test_ace_pert/
kika/sampling/testing/
kika/endf/files/
tmp_inspect/
kika/cov/legacy/

# Library dev notebooks, scratch outputs & dev notes live in the private kika-workspace repo, not here
kika/**/*.ipynb
kika/**/*.md
kika/**/output_directory/
kika/**/test_output/
nul
dir
# CLAUDE.md is a private symlink into kika-workspace (not distributed with the library)
CLAUDE.md

# AGENTS.md is the same private stub as CLAUDE.md, for the agents that read
# that name instead. Both point into kika-workspace and neither is distributed.
AGENTS.md

# The IAEA catalogue snapshot: 2 MB of scraped directory listings, built on
# demand into the user's cache directory, so neither in this history nor in the
# wheel. Read kika/endf/remote/catalog.py; build it with
#   python -m kika.scripts.build_endf_catalog
kika/endf/remote/endf_catalog.json.gz
